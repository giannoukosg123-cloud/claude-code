# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository is currently a placeholder/starter project. It contains only a `README.md` (titled "claude-code" / "my-first-project") with no source code, build configuration, dependency manifests, or tests yet.

There are no build, lint, or test commands to run because no project scaffolding exists.

## Working here

- Since there is no established structure, architecture, or convention yet, the first substantive change to this repo should set up the project scaffolding appropriate to whatever language/framework the user specifies (e.g. `package.json`, `pyproject.toml`, etc.) rather than assuming one.
- Once real code, build tooling, and tests are added, update this file to document: the actual build/lint/test commands, how to run a single test, and the high-level architecture.
